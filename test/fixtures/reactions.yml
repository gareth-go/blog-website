# == Schema Information
#
# Table name: reactions
#
#  id            :bigint           not null, primary key
#  user_id       :bigint
#  post_id       :bigint
#  reaction_type :integer
#  created_at    :datetime
#

one:
  reaction_type: 1

two:
  reaction_type: 1
